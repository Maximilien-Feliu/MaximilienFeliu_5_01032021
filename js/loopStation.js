//Here enter fuctions - objects to call

class LoopStation {

    BossRC3 () {

        for (let i = 0; i < object.length; i++) {
               
            //Here enter the fuction to loop

        }
    }

    async BossRC30 () {

        let httpRequest = await getHttpRequest(method, url, boolean);
      
        if (productId == '') {

            
            for (let i = 0; i < httpRequest.length; i++) {
                
                //Here enter the function to loop in htppRequest

            }    
            
        }else {

            //Here enter the function without needing to loop
        
        } 

    }

    async BossRC300 () {

        let httpRequest = await getHttpRequest(method, url, boolean);

        if (productId == '') {

            
            for (let i = 0; i < httpRequest.length; i++) {
                
                //Here enter the function to loop in htppRequest

            }
            
            for (let i = 0; i < object.length; i++) {

                //Here enter the function to loop in an object

            }
            
        }else {

            //Here enter the function without needing to loop

            for (let i = 0; i < object.length; i++) {

                //Here enter the function to loop in an object

            }
        
        } 
    }
}